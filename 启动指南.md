# 银龄智护 2.2 启动指南（后端改造 + 前端工程）

> 架构：物理机（Windows + HBuilderX）写前端 → 虚拟机（Ubuntu 192.168.38.134）跑后端

---

## 〇、v2.2 竞赛升级新增（本次改动）

### 1. 健康管理：手动表单 → 语音录入
- 健康页改为**大按钮语音录入** + 方言选择（普通话/四川话/粤语/河南话）
- 链路：录音上传 `/api/ai/asr` → `/api/ai/health/dispatch`（SSE 流式 AI 分析）→ 风险等级 / 工单 / 通知事件实时回显
- 历史记录带 🎤 语音 / ✍ 手动 来源标记

### 2. 首页"今日守护"AI 智能关怀
- 接口：`GET /api/health/today-care/{userId}`（`?forceRefresh=true` 强制重新生成）
- 后端聚合"今日健康记录 + 近期记录"，LLM 生成 JSON（问候语 text / 要点 bullets / 天气提示 / 服药提醒），Redis 缓存 30 分钟，Redis 不可用自动降级本地缓存；LLM 失败回退模板话术
- 新文件：`service/DailyCareService(+Impl)`、`prompts/care_today.txt`；HealthRecordMapper 新增 `selectTodayByUserId` / `selectRecentByUserId`

### 3. 反诈升级：关键词 + AI 语义 + RAG 案例库 三层流水线
- `FraudAgent` 三层检测：① 关键词防火墙 → ② AI 语义分析（prompt 预注入 RAG 案例上下文）→ ③ RAG 案例库独立比对（Chroma 新集合 `elder_fraud_case`，`ChromaUtil.searchFraudCase`）
- 风险分级：关键词命中=高；AI+RAG 双命中=高；单命中=中；否则低。检测来源 detectType：`keyword / ai / rag / ai+rag / none`
- 命中案例落库 `fraud_alert.matched_cases`（**需执行 `sql/upgrade-v21-fraud-matched-cases.sql`**）；前端 fraud.vue 展示三层流水线可视化 + 📚 RAG 命中案例

### 4. 健康风险通知链路（老人端事件 → 家属/护工 → 详情）全打通
- 后端已有：health_record 风险中/高 → 查 FamilyBind → 落库 health_notify → WebSocket 推送 `health_alert`（家属 `family:{openId}` / 护工 `caregiver:{id}`）
- 前端新增：
  - `utils/notify.js` 全局身份管理（elder/family/caregiver）+ 未读数 + tabBar 红点
  - `pages/notify/notify.vue` 通知中心（家属/护工双视角、全部/未读筛选、全部已读、下拉刷新）
  - `pages/notify/notify-detail.vue` 预警详情（风险横幅、AI 分析、🎤 老人语音原话、自动标已读）
  - `pages/profile/profile.vue` 角色切换卡片：老人模式 / 家属模式（demo_family_001/002）/ 护工模式
  - `pages/caregiver/caregiver.vue` 新增"我的健康预警"入口（未读角标 + WS `health_alert` 弹窗直达详情）
- 涉及接口：`/api/notify/inbox`、`/api/notify/{id}`、`PUT /api/notify/read/{id}`、`/api/notify/unread`、`/api/bind/by-openid/{openId}`

### 5. v2.2 数据库升级
```bash
mysql -uroot -p elderly_ai < src/main/resources/sql/upgrade-v21-fraud-matched-cases.sql
```

---

## 一、v2.1 后端改动清单（历史）

### 新增
| 模块 | 说明 |
|---|---|
| `entity/Caregiver` + Mapper + Service | 护工表（skills 技能标签 / area 区域 / current_order_count 负载） |
| `agent/OrderDispatchAgent` | **Agent5 工单调度智能体**：技能过滤→区域过滤→负载均衡 加权匹配算法，AI生成工单 + WebSocket推送 |
| `ws/OrderWebSocketHandler` + `config/WebSocketConfig` | 护工端实时接单通道 `/ws/order?caregiverId=xx` |
| `controller/CaregiverController` | 护工端接口（工单列表/工作台统计） |
| `util/ChatCacheUtil` | Redis 高频问答缓存（24h，Redis不可用自动降级） |
| `/api/ai/asr` | 录音→文本（小程序两步式语音链路第一步） |
| `/api/ai/text/dispatch` | 文字统一调度入口（与语音同一套路由，SSE事件流） |
| `/api/order/smart-dispatch` | 智能派单接口 |
| `/api/report/dashboard` | 数据看板（今日统计+护工排行+近7天日报） |
| `sql/upgrade-v2.sql` | 已有数据库的增量升级脚本 |

### 修改
- **SSE 协议规范化**：所有流式接口统一返回 `Flux<ServerSentEvent>`，事件类型：`asr / intent / token / risk / order / emotion / done / error`（原实现手动拼 `data:` 前缀会与MVC自动包装叠加造成双前缀）
- **Agent1 语音链路**：userId 全链路透传；对话落库 chat_log；意图分类容错归一化
- **Agent2 健康链路**：新增心率正则、血糖红线；语音健康数据自动落库 health_record；**高危预警自动联动 Agent5 生成紧急工单**（文档演示场景一）；Chroma 不可用时降级直答
- **Agent3 反诈链路**：关键词库扩充至16个；命中**落库 fraud_alert**（备赛数据佐证）；fraud.txt 增加 7 组 Few-Shot；反诈顺口溜语音播报
- **Agent4 情感链路**：情绪标签返回前端；低落走专属安抚话术；正常闲聊走 Redis 缓存
- **日报修复**：工单统计由"待处理数"改为按时间范围统计；新增"接单最多护工"；定时任务改为每日 23:00
- **TTS 接口**：`R<byte[]>` JSON包音频改为返回 base64（前端写临时文件即可播放）
- **Redis 配置**：`spring.redis.*` → `spring.data.redis.*`（Spring Boot 3 规范）

### 删除
- `util/AliAiUtil`（与 LlmUtil 重复的死代码，且是虚拟机上启动报错的直接原因——`Could not resolve placeholder 'ai.aliyun.access-key-secret'`）
- `util/SseUtil`（60ms 假延迟已被真实流式输出替代）

---

## 二、后端部署（虚拟机 192.168.38.134）

### 1. 同步代码
把整个后端目录传到虚拟机（或 git pull），替换旧的 `silver-care-ai` 工程。

### 2. 数据库升级（重要！）
```bash
mysql -uroot -p elderly_ai < src/main/resources/sql/upgrade-v2.sql
```
（全新环境则直接执行 `schema.sql`，含全部表+种子数据）

### 3. 检查 application.properties
- MySQL：`192.168.38.134:3306`（保持原样即可）
- Redis：`127.0.0.1:6379` —— 需要虚拟机上装 Redis：`sudo apt install redis-server`
  - 没装也不影响运行，只是问答缓存不生效（自动降级）
- Chroma：`127.0.0.1:8001` —— 按原教程启动 chroma server

### 4. 编译运行
```bash
cd silver-care-ai
mvn clean package -DskipTests
java -jar target/silver-care-ai-2.0.jar
```

### 5. 验证
```bash
curl http://192.168.38.134:8080/api/hello
# → {"code":200,"data":{"msg":"银龄智护服务运行正常！"...}}

# 测试文字统一调度（SSE流式）
curl -N -X POST http://192.168.38.134:8080/api/ai/text/dispatch \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"message":"俺今个头晕，血压180了，咋整？"}'
```

---

## 三、前端部署（物理机 HBuilderX）

### 1. 导入工程
HBuilderX → 文件 → 导入 → 从本地目录导入 → 选择 `silver-care-frontend` 目录。

### 2. 运行
- **微信小程序**：运行 → 运行到小程序模拟器 → 微信开发者工具
  - 首次需在微信开发者工具中打开"不校验合法域名"（manifest 已配 `urlCheck:false`）
- **H5**：运行 → 运行到浏览器（录音功能仅小程序端支持，H5 用文字输入）

### 3. 换 IP
后端 IP 如有变化，只需改一个文件：`utils/config.js` 里的 `BASE_URL` 和 `WS_URL`。

### 4. 微信 AppID
`manifest.json` 中 `mp-weixin.appid` 当前为游客模式 `touristappid`，正式演示时换成你自己的测试号 AppID。

---

## 四、页面与后端接口对照

| 页面 | 功能 | 接口 |
|---|---|---|
| 首页 | 服务状态/今日概况/**今日守护AI关怀**/五大智能体入口 | `/api/hello`、`/api/report/dashboard`、`/api/health/today-care/{userId}` |
| 语音对话 | 录音(PCM 16k)→方言ASR→SSE打字机→TTS播报→反诈全屏警告→高危自动工单 | `/api/ai/asr`、`/api/ai/text/dispatch`、`/api/ai/tts` |
| 健康管理 | **语音录入**（方言选择）→SSE流式AI分析→风险/工单/通知实时回显→历史(🎤/✍来源标记) | `/api/ai/asr`、`/api/ai/health/dispatch`、`/api/health/list` |
| 呼叫护工 | 需求输入→AI工单→加权调度派单 | `/api/order/smart-dispatch`、`/api/order/list` |
| 护工端 | 选择护工身份→WebSocket实时接单弹窗→完成工单→**我的健康预警入口(未读角标+health_alert弹窗)** | `WS /ws/order`、`/api/caregiver/*`、`/api/order/status`、`/api/notify/unread` |
| 反诈记录 | 三层流水线可视化(关键词→AI→RAG)+检测来源徽标+**RAG命中案例**+顺口溜播报 | `/api/fraud/list`、`/api/ai/tts` |
| **通知中心** | 家属/护工双视角预警列表(未读筛选/全部已读/下拉刷新) | `/api/notify/inbox`、`/api/notify/unread`、`/api/bind/by-openid/{openId}` |
| **预警详情** | 风险横幅/AI分析/🎤老人语音原话/自动标已读/电话回访 | `/api/notify/{id}`、`PUT /api/notify/read/{id}` |
| 数据看板 | 今日统计/护工排行/AI日报 | `/api/report/dashboard`、`/api/report/generate` |
| 我的 | 档案编辑 + **角色切换(老人/家属demo_family_001·002/护工)** + 通知中心入口 | `/api/user/update`、`/api/user/login`、`/api/bind/by-openid/{openId}` |

---

## 五、备赛演示场景速查

1. **方言+健康+RAG**（语音对话页，选"四川话"）：
   说"俺今个头晕，血压180了，咋整？" → 高危预警 → 自动生成紧急工单（指派急救护工）
2. **反诈**（语音对话页文字输入）：
   输入"那个药说能根治糖尿病，才500块" → 全屏红色警告 + 顺口溜播报 + fraud_alert 落库
3. **工单调度**（护工端页选"王强"并连接，另一台设备或同机在呼叫护工页提交"需要康复按摩"）：
   工单自动分配给技能匹配且负载最低的护工，护工端弹窗"叮"一声
4. **健康语音录入 + 通知链路**（v2.2 新增，完整闭环演示）：
   ① 健康页选方言，按住说话："这两天心慌，心跳大概110，有点累" → 松开即出 ASR 文本 + AI 流式分析
   ② 若判为中/高风险：页面出现 🚨 风险提醒 + 📨 通知摘要（服务端已查 FamilyBind 并落库 health_notify、推送 WebSocket）
   ③ "我的"页切换为家属身份（demo_family_001）→ 通知中心红点/角标 → 进入通知中心看列表 → 点开详情看风险横幅、AI 分析、🎤 老人原话 → 自动标已读
   ④ 护工端页同样有"我的健康预警"入口，在线时收到 `health_alert` 弹窗可直达详情
5. **今日守护**（首页）：
   首页"✨ 今日守护"卡片展示 AI 生成的个性化关怀（问候+健康小结+饮水/服药提醒）；点"换个说法"重新生成（演示缓存+重生成两种状态）
